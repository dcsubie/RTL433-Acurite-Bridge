# Local development MQTT broker for the RTL433 Acurite Bridge.
# Dev-only: binds to localhost and allows anonymous connections so the
# Python bridge and mosquitto_pub/sub can be exercised without credentials.
# This is NOT a production configuration.
listener 1883 127.0.0.1
allow_anonymous true
persistence false
log_dest stdout
