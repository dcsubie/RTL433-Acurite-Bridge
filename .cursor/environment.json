{
  "name": "RTL433 Acurite Bridge (dev)",
  "user": "ubuntu",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends mosquitto mosquitto-clients rtl-433 python3-venv && python3 -m venv .venv && ./.venv/bin/pip install --upgrade pip && ./.venv/bin/pip install -r addon/requirements.txt",
  "terminals": [
    {
      "name": "mqtt-broker",
      "command": "mosquitto -c .cursor/mosquitto.conf",
      "description": "Local Mosquitto MQTT broker on 127.0.0.1:1883 for testing the bridge (anonymous access, dev only)."
    }
  ],
  "ports": [
    {
      "name": "mqtt",
      "port": 1883
    }
  ]
}
